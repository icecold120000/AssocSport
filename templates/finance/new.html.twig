{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouveau flux{% endblock %}

{% block body %}
    <h1>Ajouter un nouveau flux</h1>

    {% for message in app.flashes('SuccessFlux') %}
        <div class="flash-success">
            {{ message }}
        </div>
    {% endfor %}
    <div>
        <div class="container-fluid formEntité2" id="formFlux">
            {{ include('finance/_form.html.twig') }}
        </div>
    </div>
    <div>
        <a href="{{ path('finance_index') }}">
            <button class="btn btn-secondary" id="btnRetour">
                Retourner dans la liste 
            </button>
        </a>
    </div>
    
{% endblock %}
