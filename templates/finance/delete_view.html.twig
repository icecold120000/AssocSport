{% extends 'base.html.twig' %}

{% block title %}Flux{% endblock %}

{% block body %}
    <h1>Suppression</h1>

    <p>Cliquez sur le bouton Supprimer pour supprimer le flux choisi : {{ flux.libelleFlux}}</p>


    {{ include('finance/_delete_form.html.twig') }}

	<div>
        <a href="{{ path('finance_index') }}">
            <button class="btn btn-secondary" id="btnRetour">
                Retourner dans la liste 
            </button>
        </a>
    </div>
{% endblock %}
