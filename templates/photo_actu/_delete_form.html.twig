<form method="post" action="{{ path('photo_actu_delete', {'id': photo_actu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photo_actu.id) }}">
    <button class="btn">Delete</button>
</form>
