{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouveau budget{% endblock %}

{% block body %}
    <h1>Ajouter un nouveau budget</h1>

    {% for message in app.flashes('SuccessBudget') %}
        <div class="flash-success">
            {{ message }}
        </div>
    {% endfor %}

    <div>
        <div class="container-fluid formEntité">
            {{ include('budget/_form.html.twig') }}
        </div>
    </div>
    <div>
        <a href="{{ path('budget_index') }}">
        	<button class="btn btn-secondary" id="btnRetour">
                Revenir dans la liste
            </button> 
        </a>
    <div>
{% endblock %}